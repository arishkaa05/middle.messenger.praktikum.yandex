<div class="input{{#if className}} {{className}}{{/if}}">
  <input
    class="input__element{{#if className}} {{className}}{{/if}}"
    type="{{ type }}" title="{{ title }}" name="{{ name }}" value="{{value}}" autocomplete="on">
</div>
