<div class="chat-page__list-item">
  <div class="user__img"></div>
  <div style="width: 100%">
    <div class="chat-page__user-sent">
      <p class="user__name">{{title}}</p>
      <p class="chat-page__user-time">{{time}}</p>
    </div>
    {{#if unread_count}}
      
      <div class="chat-page__user-msg">
        <div class="chat-page__message"> {{last_message.content}}</div>
        <div class="chat-page__count">{{unread_count}}</div>
      </div>
    {{/if}}
    {{#unless unread_count}}
      <div class="chat-page__user-msg">
        <div class="chat-page__message">
          <span>Вы:</span>
          {{last_message.content}}
        </div>
      </div>
    {{/unless}}
  </div>
</div>
